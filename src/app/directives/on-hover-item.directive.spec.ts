import { OnHoverItemDirective } from './on-hover-item.directive';

describe('OnHoverItemDirective', () => {
  it('should create an instance', () => {
    const directive = new OnHoverItemDirective();
    expect(directive).toBeTruthy();
  });
});
